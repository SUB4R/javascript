/*-------------------------OPERATUER NUMERIQUE------------------------ */


// +, -, *, % , **

// declarer une constante operation a qui Affecte l'addition de 6786786 et 78798
// Affiche en console le resultat du calcul est OPERATION

let operation = 6786786 + 78798  // Addition
console.log(`le résultat du calcul est : ${operation}`);

//soustraction , multiplication et division

 operation = 6786786 - 78798 // Soustraction
console.log(`le résultat du calcul est : ${operation}`);

operation = 6786786 * 78798 // Multiiplication
console.log(`le résultat du calcul est : ${operation}`);

operation = 6786786 / 78798 // Division
console.log(`le résultat du calcul est : ${operation}`);

operation = 10 % 3; // Modulo (reste de la division)
// console.log(`le résultat du calcul est : ${operation}`);

operation = 3 ** 6;
console.log(`le résultat du calcul est : ${operation}`);

const response = prompt ('Donne moi un nombre')
const response2 = prompt ('Donne moi un autre nombre')

if (response %response2 == 0){
    console.log ( `${response2} est un multiple de ${response} 3`);
}else{
    console.log((faux))
}

// demande à l'utilisateur un nombre
// demander à l'utilisateur un deuxieme nombre

// si le nombre est divisible par le premier
// affichez ce nombre est un multiple du deuxieme
// Sinon
// ce n'est pas un multiple

// soustraction, multiplication et division 

